import { letterCombinations } from "../src/letterCombinationsOfAPhoneNumber";

describe("test Letter Combinations of a Phone Number", () => {
  it("", () => {
    expect(letterCombinations("23")).toEqual(["ad", "ae", "af", "bd", "be", "bf", "cd", "ce", "cf"]);
  });
});
